# SGN Shipping — Frontend

React + Vite frontend for SGN Shipping. See the [root README](../README.md)
for what this project does and how to run the full stack.

## Development

```bash
npm install
npm run dev
```

Requires the backend running on `http://localhost:8080` (see the root
README for both Docker Compose and manual setup).
